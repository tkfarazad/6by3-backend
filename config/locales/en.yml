en:
  hello: "Hello world"
  errors:
    email?: '%{value} is invalid email'
    image?: '%{value} is invalid image'
    video?: '%{value} is invalid video'
    confirm_email:
      create:
        token_invalid: Confirmation token is invalid.
        token_expired: Confirmation token has been expired.
    change_password:
      create:
        token_invalid: Reset password token is invalid.
        token_expired: Reset password token has been expired.
